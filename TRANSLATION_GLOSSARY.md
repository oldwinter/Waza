# Waza 中文翻译术语表

本文件用于保持中文翻译一致。代码、命令、路径、配置键、URL、package 名、API 名、品牌/产品/人名/组织/仓库名、协议名和 License 法律表达保持原文。

| English | 中文策略 |
|---|---|
| Waza / Kaku / Kami | 保留原文，括号内可保留日文原义 |
| skill / skills | 保留 `skill` / `skills`，必要时译作“技能”解释 |
| rule / rules | 保留 `rule` / `rules`，必要时译作“规则”解释 |
| script / scripts | 保留 `script` / `scripts`，必要时译作“脚本”解释 |
| resolver / routing | 译作“路由器” / “路由”，文件名和标题保留原文 |
| marketplace | 保留 `marketplace` |
| frontmatter | 保留 `frontmatter` |
| preflight | 保留 `preflight` |
| gotcha / gotchas | 保留 `gotcha` / `gotchas` |
| smoke test | 译作 `smoke 测试` |
| artifact | 保留 `artifact`，必要时译作“产物”解释 |
| generated | 保留 `generated`，必要时译作“生成的”解释 |
| release / publish / push | 保留原文动词，避免改变操作含义 |
| root cause / fix / diff | 保留原文，中文语境中直接使用 |
| Claude Code / Codex / Claude Desktop / Pi | 保留原文 |
| slash command | 保留 `slash command` |
| AI agent / agent | 保留 `agent` / `AI agent` |
| durable context / memory | 保留原文，必要时补充“持久上下文” |
| prompt / guide / playbook | 保留原文，必要时补充中文解释 |
